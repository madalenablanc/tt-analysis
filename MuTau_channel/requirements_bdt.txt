# Python BDT Framework Requirements for MuTau Channel
# Install with: pip install -r requirements_bdt.txt

# Core ML libraries
xgboost>=1.7.0
scikit-learn>=1.0.0

# Data handling
numpy>=1.21.0
pandas>=1.3.0

# Plotting
matplotlib>=3.4.0
seaborn>=0.11.0

# ROOT file handling (choose one):
# Option 1: uproot (recommended - faster, no ROOT installation needed)
uproot>=5.0.0
awkward>=2.0.0

# Option 2: PyROOT (if you have ROOT installed)
# No pip install needed, comes with ROOT

# Optional but recommended
jupyter>=1.0.0  # For interactive analysis
